# Z-aat

This repository contains a small prototype for a retro top-down RPG built with Pygame. It demonstrates simple movement.

## Requirements
- Python 3.12+
- `pygame`
- `networkx`

## Tarot Knowledge Graph
The `eqtarot` package provides a simple loader that builds a multi-edge graph
from JSON and CSV data. Use the CLI to display the sample graph:
```
python -m eqtarot.cli
```
